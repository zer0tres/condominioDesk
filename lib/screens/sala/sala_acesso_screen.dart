import 'package:flutter/material.dart';

class SalaAcessoScreen extends StatelessWidget {
  const SalaAcessoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Acesso da Sala')),
      body: const Center(child: Text('Acesso por código — em construção')),
    );
  }
}
